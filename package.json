{
	"name": "cocrear",
	"version": "1.0.0",
	"license": "MIT",
	"source": "src/index.html",
	"scripts": {
		"dev": "parcel",
		"build": "parcel build --dist-dir build --public-url /co-crear",
		"deploy": "gh-pages -d build",
		"build-and-deploy": "yarn build && yarn deploy"
	},
	"dependencies": {
		"@types/node": "^18.0.0",
		"@vaadin/router": "^1.7.4",
		"gh-pages": "^4.0.0",
		"parcel": "^2.6.2"
	}
}
